enum LocationMode { highAccuracy, batterySaving, deviceSensors }

enum LocationPurpose { none, signIn, transport, sport }

enum DesiredAccuracy {
  best,
  bestForNavigation,
  nearestTenMeters,
  kilometer,
  threeKilometers,
}

enum GeoLanguage { defaultLanguage, chinese, english }

enum LocationProtocol { http, https }
